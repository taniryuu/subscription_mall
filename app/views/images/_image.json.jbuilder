json.extract! image, :id, :image_subscription, :image_interview, :comment, :time, :user_id, :owner_id, :subscription_id, :interview_id, :blog_id, :created_at, :updated_at
json.url image_url(image, format: :json)
