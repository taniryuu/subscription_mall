<%= form_with(model: shop, local: true) do |form| %>
  <% if shop.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shop.errors.count, "error") %> prohibited this shop from being saved:</h2>

      <ul>
      <% shop.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :category_name %>
    <%= form.text_field :category_name, id: :shop_category_name %>
  </div>

  <div class="field">
    <%= form.label :monthly_fee %>
    <%= form.select :monthly_fee, Shop.monthly_fees.keys, { include_blank: true } %>
  </div>

  <div class="field">
    <%= form.label :phone_number %>
    <%= shop.phone_number %>
  </div>

  <div class="field">
    <%= form.label :store_information %>
    <%= form.text_field :store_information, id: :shop_store_information %>
  </div>

  <div class="field">
    <%= form.label :payee %>
    <%= form.text_field :payee, id: :shop_payee %>
  </div>

  <div class="field">
    <%= form.label :line_id %>
    <%= form.text_field :line_id, id: :shop_line_id %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :shop_address %>
  </div>

  <div class="field">
    <%= form.label :owner_id %>
    <%= form.text_field :owner_id, id: :shop_owner_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
