<% if object.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      入力してない箇所が<%= pluralize(object.errors.count,"つあります。") %>
    </div>
    <ul>
    <% object.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>