<%= form_with(model: blog, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_title, placeholder: "例)美味しいラーメン", class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :blog_body, placeholder: "例)美味しいラーメン", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= form.label :image_photo %>
    <%= form.file_field :image_photo %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
