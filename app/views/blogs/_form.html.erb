<%= form_with(model: blog, multipart: true, local: true) do |form| %>
  <% if blog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(blog.errors.count, "error") %> prohibited this blog from being saved:</h2>

      <ul>
      <% blog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :blog_title, placeholder: "例)美味しいラーメン", class: 'form-control' %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body, id: :blog_body, placeholder: "例)美味しいラーメン", class: 'form-control' %>
  </div>
  <div class="form-group">
    <%= form.label :"写真を選択してください", for: "file-input", type:"file", class: "file_field" %>	  
    <%= form.file_field :image_photo, id: "file-input" %>
    <!--%= form.label :image_photo, for: "file-input", type:"file", class: "fa fa-file-picture-o" %-->
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
